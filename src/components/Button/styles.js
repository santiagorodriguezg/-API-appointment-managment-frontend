import { Button } from 'antd';
import styled from 'styled-components';

export const StyledButton = styled(Button)`
  &,
  &:hover,
  &:focus {
    width: 100%;
    background: #9d429f;
    border-color: #9d429f;
  }
`;
