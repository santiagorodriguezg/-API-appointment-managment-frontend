import { Layout } from 'antd';
import styled from 'styled-components';

const { Footer } = Layout;
const S = {};

S.Footer = styled(Footer)`
  text-align: center;
  padding: 30px 0 0 0;
`;

export default S;
